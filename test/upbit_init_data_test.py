import unittest
import pandas as pd
import pyupbit
from datetime import datetime
import json

class TestCalculator(unittest.TestCase):
    #def test_time(self):
        
    
    # def test_data(self):
    #     df = pyupbit.get_ohlcv("KRW-BTC", interval="minute1", count=6, to="202001010006")
    #     df.reset_index(inplace=True)
    #     df.rename(columns={'index': 'timestamp'}, inplace=True)
    #     df['timestamp'] = df['timestamp'].apply(lambda x: x.isoformat())
    #     json_data = df.to_json(orient='records', date_format='iso')
    #     data_points = json.loads(json_data)
        
    #     self.assertEqual(data_points[0].open, '2020')
    # def test_add(self):
    #     self.assertEqual(Add, 3)
    #     self.assertEqual(Add(-1, 1), 0)

    # def test_subtract(self):
    #     self.assertEqual(Substract(10, 5), 5)
    #     self.assertEqual(Substract(-1, -1), 0)
    def ssssss(self):
        self.assertTrue(True)
if __name__ == '__main__':
    unittest.main()